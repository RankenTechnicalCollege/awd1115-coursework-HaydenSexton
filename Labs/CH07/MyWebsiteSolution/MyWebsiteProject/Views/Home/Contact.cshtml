@model IDictionary<string, string>
@{
    ViewData["Title"] = "Contact";
}

@section header {
    <h1>Contact Me</h1>
}

<table class="table">
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.Key</td>
            <td>@item.Value</td>
        </tr>
    }
</table>
